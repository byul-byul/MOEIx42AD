# /backend/requirements-ml.txt — Phase 4+ (Voice + Sentiment)
# Install separately: pip install -r requirements-ml.txt

# Voice STT (requires ffmpeg system dep)
openai-whisper==20231117

# Voice TTS
gTTS==2.5.1

# Sentiment / Arabic NLU
transformers==4.41.1
torch==2.3.0
